services:
  mcp-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - RESTOPLACE_KEY=${RESTOPLACE_KEY}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - PORT=3000
    restart: unless-stopped
